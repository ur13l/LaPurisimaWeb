/**
 * Uriel Infante
 * Archivo para definir las funciones del dataTables en la vista de Promociones.
 */

$(function(){
    var tablaPromociones = $("#table-stock").DataTable(generarTablaStock("promo"));
});
